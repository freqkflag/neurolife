import { TinyNextActionCard } from '@neurolife/design-system';

export default function TasksPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-6">Tasks</h1>
      <TinyNextActionCard action="Only one task matters right now." />
    </div>
  );
}
